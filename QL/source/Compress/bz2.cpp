#include "../Pixmap/pixmap.hpp"
#include "compressors.hpp"

#include <bzlib.h>

ql::Compress::Data ql::Compress::bz2::Compress(void* data, size_t size) {
    ql::Compress::Data ret = {0,0};
    if(data != NULL) {
        
    }
    return ret;
}