#pragma once

namespace ql {
    /* Gets version of the library */

    const char* getVersion();

    /* Gets name of the library*/

    const char* getName();

    /* Gets release version of the library*/

    const char* getRelease();
}